import React from 'react'

const Project1: React.FC = () => {
	return (
		<></>
		// <Stack sx={(theme) => ({height: 500, backgroundColor: theme.palette.background.paper})}>
		// 	<Typography variant="h2">Satisfactory Calculator</Typography>
		// 	<Typography variant="h7">The app is designed optimize production lines in the game Satisfactory, helping players minimize resource usage, and maximize efficiency.
		// 	</Typography>
		// </Stack>
	)
}

export default Project1